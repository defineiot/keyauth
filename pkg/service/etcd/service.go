package etcd

const (
	servicePrefix       = "/service"
	componentsKeySuffix = "components"
	featuresKeySuffix   = "features"
)

// ServiceManager is use mongodb as storage
type ServiceManager struct {
}
